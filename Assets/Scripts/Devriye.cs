using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.AI;

public class Devriye : MonoBehaviour
{
    // Start is called before the first frame update
    // public Transform[] noktaList;
    
    void Start()
    {

        
        



    }

    // Update is called once per frame
    void Update()
    {
        // rotasyon kitliyor sadece rigidbody boazabilir onda da rotation kitleme transform.eulerAngles = new Vector3(0, 0, 0);
        //if (Karakterimiz.transform.position.x < this.transform.position.x)
        //{
        //this.transform.localScale = new Vector2(Xscalem, transform.localScale.y);
        //}
        //else
        //{
        //    this.transform.localScale = new Vector2(-Xscalem, transform.localScale.y);
        //}
      


    }
}












































